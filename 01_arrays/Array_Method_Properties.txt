1. includes() - method of Array instances determines whether an array includes a certain value among its entries, returning true or false as appropriate.

2. split() - method splits a string into an array of substrings.does not change the original string. returns the new array.